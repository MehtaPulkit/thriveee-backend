// booking-status.enum.ts
export enum BookingStatus {
    DRAFT = 'draft',
    SCHEDULED = 'scheduled',
    ASSIGNED = 'assigned',
    IN_PROGRESS = 'in_progress',
    COMPLETED = 'completed',
    CANCELLED = 'cancelled',
    REFUNDED = 'refunded',
}
